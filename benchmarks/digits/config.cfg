[SMAC]
p = params_random_forest.pcs
num_ei_random = 10000

[TPE]
space = space.py

[SPEARMINT]
config = config.pb

[HPOLIB]
console_output_delay = 2.0
dispatcher = runsolver_wrapper.py
number_of_jobs = 20
result_on_terminate = 1
function = python ../../wrapper.py --dataset digits --data_dir /data/aad/automl_data
